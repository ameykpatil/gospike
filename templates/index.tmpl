<html>
<head>
    <title>GoSpike</title>
    <link rel="stylesheet" href="/templates/style.css">
</head>
<body>
    <h1>GoSpike</h1>
    <div class="page-wrapper">
    <h1>Connect To Aerospike</h1>
    <form id="connect" action="/connect" method="get">
        <div class="field">
            <label for="host">Host:</label>
            <input type="text" id="host" name="host" />
        </div>
        <div class="field">
            <label for="port">Port:</label>
            <input type="text" id="port" name="port" />
        </div>
        <div class="field">
            <button type="submit">Connect</button>
        </div>
    </form>
    <div id="connect-response" class="hideerror">{{.message}}</div>
    </div>

</body>
</html>